spring:
    application:
        name: ecommerce

    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.wtfgsjuopucxobcaeklu&password=SAVEITFORLATER1247
        username: postgres.wtfgsjuopucxobcaeklu
        password: SAVEITFORLATER1247
    jpa:
        # show-sql: true
        hibernate:
            ddl-auto: update 
            # naming:
            #     physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        properties:
            hibernate:
                # '[format_sql]': true

    flyway:
        baseline-on-migrate: true
        
server:
    port: 8080